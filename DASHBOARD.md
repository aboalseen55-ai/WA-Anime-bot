# لوحة Sam Bot

المسار: `/dashboard`. تعمل اللوحة في نفس عملية البوت وعلى `PORT`، وتتوقف واجهاتها عندما ينقطع اتصال واتساب. ينهي Railway اتصال HTTPS؛ لا تفتح منفذ التطبيق مباشرة على الإنترنت.

## الإعدادات

- `DASHBOARD_ADMIN_PASSWORD`: كلمة مرور قوية خاصة بمالك اللوحة.
- `DASHBOARD_ENCRYPTION_KEY`: مفتاح سري عشوائي ثابت لتشفير مفاتيح الخدمات. تغيير هذا المفتاح يمنع فك تشفير الخدمات المحفوظة سابقًا.
- يستخدم التطبيق اتصال `MONGO_URI` نفسه. لا تُرسل بيانات اتصال MongoDB إلى المتصفح.

تُنشئ Mongoose مجموعة `dashboardtemplates` عند أول تعديل؛ لا يلزم SQL أو تعديل يدوي للقاعدة. النصوص الافتراضية تأتي من ملفات البوت، والتعديلات فقط تُحفظ في MongoDB. استعادة النص الأصلي تحذف التعديل، ولا تحذف كود الأمر.

## الأقسام

- نظرة عامة: عدادات النشاط الحالية، XP، النجوم، ومتصدّرو الألعاب.
- الأوامر والردود: دليل الأوامر الأصلية، محرر النصوص والقوالب مع المتغيرات والمعاينة والاسترجاع، وأوامر مخصصة مع صلاحيات وخدمات اختيارية.
- الممالك: إنشاء وتعديل وحذف، ربط المجموعات التي انضم إليها البوت، أدوار المجموعات، الإدارة، روابط الدعوات، والمنطقة الزمنية. الحذف يتطلب كتابة الاسم، ويحذف بيانات المملكة التابعة وفق خدمة الحذف الحالية.
- الأعضاء: بحث وصفحات داخل المملكة؛ تفاصيل النشاط وتعديل اللقب والصلاحية والحظر داخل البوت. هذا لا يغيّر صلاحيات واتساب نفسها.
- إنجازات الإدارة: العدّادات الحالية ومعاينة رسالة تقرير كل إداري.
- الألعاب: جلسات المافيا وسجل فعاليات الألعاب المخزّن. أدوار المافيا السرية ليست مكشوفة.
- البنوك: أرصدة الممالك وسجل العمليات المتاح؛ لا تُعدّل الأرصدة خارج منطق البنك.
- الخدمات: GET/POST عبر HTTPS، تشفير Headers وBody، وحفظ المفاتيح القديمة عند ترك الحقول فارغة. `{}` يفرغ الحقل صراحةً.
- استهلاك الذكاء: سجلات الطلبات والتوكنز المتوفرة على مستوى البوت.
- سجل التغييرات: أحداث الممالك والتعديلات من اللوحة، دون كشف رموز التسجيل أو الأسرار.

زر إعادة تشغيل البوت يرسل استجابة نجاح ثم ينهي عملية Node؛ Railway يعيد تشغيل الخدمة تلقائيًا. لا يحذف MongoDB أو مجلد جلسة واتساب، لكنه يسبب انقطاعًا قصيرًا للاتصال.

## حدود واضحة

- لا يتغير منطق الألعاب أو صلاحيات الأوامر الأصلية عند تعديل نص الرد.
- بعض الردود المركّبة تظهر كمقاطع منفصلة؛ المتغيرات المطلوبة لا يمكن حذفها. النص الناتج بالكامل من بيانات أو من AI ليس قالبًا ثابتًا يمكن تحريره هنا.
- القوالب والأوامر المخصصة والخدمات إعدادات عامة لكل البوت، وليست مخصصة للمملكة المختارة.
- عدادات التفاعل اليومية ليست أرشيفًا للأيام السابقة. تعرض اللوحة البيانات التي يحتفظ بها البوت بالفعل، ولا تختلق تاريخًا غير مخزّن.
- منع SSRF يرفض الشبكات الداخلية وإعادة التوجيه، ويثبت نتيجة DNS عند الطلب. مفاتيح الخدمات لا تُعاد للمتصفح.
- الجلسات محدودة بثماني ساعات وتُلغى عند إعادة تشغيل العملية؛ إعدادات البوت والقوالب لا تُفقد.
- حساب المالك واحد، وكل عمليات اللوحة تُسجل بهوية `dashboard:owner`. لا توجد حسابات متعددة منفصلة.

## التحقق المحلي

`npm test` يشغّل فحوص الصياغة واختبارات المصادقة وCSRF والتحقق من المدخلات والقوالب وحماية الخدمات.

`node scripts/verifyDashboardMigration.js a277aa0` يقارن تركيب الكود قبل ربط القوالب وبعده، بعد إزالة أغلفة التنسيق، للتأكد من بقاء منطق البوت كما هو.

`node tests/dashboardFixture.js` يشغّل معاينة محلية ببيانات وهمية على `http://127.0.0.1:4187/dashboard`، بكلمة مرور `dashboard-test-only`. لا يتصل بواتساب ولا MongoDB ولا يكتب بيانات الإنتاج. لا تشغّله كخادم الإنتاج.

مكتبة `acorn` تُستخدم لقراءة دليل الأوامر دون تنفيذ ملفاتها، و`lucide` للأيقونات المحلية، دون تحميل مكتبات من طرف ثالث داخل المتصفح.
