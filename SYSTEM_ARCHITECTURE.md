# 🔧 دليل آلية عمل النظام التفاعلي للأوامر

## نظرة عامة
تم تطوير نظام جديد لقوائم الأوامر يوفر تجربة تفاعلية منظمة وسهلة الاستخدام لمستخدمي البوت.

---

## 🏗️ البنية الهندسية

### المكونات الأساسية

#### 1. **أمر /أوامر**
```javascript
/أوامر
```
- يدخل المستخدم الحالة `awaitingCommandsChoice`
- يتلقى القائمة الرئيسية
- ينتظر اختيار رقم (1-7)

#### 2. **معالج الاختيارات**
```javascript
handleCommandsChoice()
```
- يعالج الأرقام من 1 إلى 7
- يستدعي الدالة المناسبة حسب الرقم
- يحذف المستخدم من قائمة الانتظار

#### 3. **دوال عرض القوائم**
```javascript
showMemberCommands()        // فئة 1
showGamesCommands()         // فئة 2
showBankCommands()          // فئة 3
showModeratorCommands()     // فئة 4
showAdminCommands()         // فئة 5
showRanksSystem()           // فئة 6
showSearchCommands()        // فئة 7
```

---

## 🔄 آلية التشغيل

### مسار التنفيذ

```
المستخدم يكتب /أوامر
         ↓
handleAdminCommands() يكتشف الأمر
         ↓
showCommandsList() تعرض القائمة الرئيسية
         ↓
المستخدم يضاف إلى awaitingCommandsChoice
         ↓
messageHandler ينتظر رقماً من 1-7
         ↓
إذا الرقم صحيح: handleCommandsChoice() تستدعى
         ↓
دالة العرض المناسبة تنفذ
         ↓
المستخدم يُزال من awaitingCommandsChoice
```

### رسم توضيحي

```
┌────────────────────────────────────────────────┐
│            مستخدم يكتب /أوامر                  │
└────────────────────────────────────────────────┘
                    ↓
┌────────────────────────────────────────────────┐
│      showCommandsList() - القائمة الرئيسية     │
│          (7 خيارات مع الصلاحيات)              │
└────────────────────────────────────────────────┘
                    ↓
        ┌──────────────────────────┐
        │ awaitingCommandsChoice   │
        │  يحتوي على JID المستخدم│
        └──────────────────────────┘
                    ↓
        ┌──────────────────────────┐
        │ messageHandler ينتظر     │
        │ رقماً من 1-7             │
        └──────────────────────────┘
                    ↓
            يكتب المستخدم (1-7)
                    ↓
                مختارات:
    ┌─────────────────────────────────────┐
    │ 1 → showMemberCommands()            │
    │ 2 → showGamesCommands()             │
    │ 3 → showBankCommands()              │
    │ 4 → showModeratorCommands()         │
    │ 5 → showAdminCommands()             │
    │ 6 → showRanksSystem()               │
    │ 7 → showSearchCommands()            │
    └─────────────────────────────────────┘
                    ↓
        ┌──────────────────────────┐
        │  يُحذف من awaitingChoice │
        │  العملية تنتهي           │
        └──────────────────────────┘
```

---

## 📂 الملفات المستخدمة

### 1. `commands/commandsList.js`
**الملف الرئيسي الجديد** - يحتوي على:
- `showCommandsList()` - عرض القائمة الرئيسية
- `showMemberCommands()`
- `showGamesCommands()`
- `showBankCommands()`
- `showModeratorCommands()`
- `showAdminCommands()`
- `showRanksSystem()`
- `showSearchCommands()`
- `handleCommandsChoice()` - معالجة الاختيارات

### 2. `commands/adminCommands.js`
**تعديلات على الملف الحالي**:
```javascript
// استيراد الدوال الجديدة
import { showCommandsList, handleCommandsChoice } from "./commandsList.js";

// في أمر /أوامر
if (command === '/أوامر') {
    await showCommandsList(sock, jid, sender);
    return;
}
```

### 3. `handlers/messageHandler.js`
**تعديلات على معالج الرسائل**:
```javascript
// استيراد
import { handleCommandsChoice } from "../commands/commandsList.js";

// متغير جديد
export const awaitingCommandsChoice = new Set();

// في handleAdminCommands
if (text.toLowerCase() === "/أوامر") {
    awaitingCommandsChoice.add(sender);
}

// معالجة الاختيارات
if (awaitingCommandsChoice.has(sender) && /^[1-7]$/.test(text.trim())) {
    await handleCommandsChoice(sock, jid, sender, text);
    awaitingCommandsChoice.delete(sender);
    return;
}
```

---

## 🔐 نظام الصلاحيات

### فحص الصلاحيات

```javascript
// في كل دالة عرض قوائم المشرفين
const userIsModerator = await isModerator(sender);
if (!userIsModerator) {
    await sock.sendMessage(jid, { 
        text: '❌ هذه الأوامر متاحة للمشرفين والأدمنز فقط!' 
    });
    return;
}

// في كل دالة عرض قوائم الأدمن
const userIsAdmin = await isAdmin(sender);
if (!userIsAdmin) {
    await sock.sendMessage(jid, { 
        text: '❌ هذه الأوامر متاحة للأدمن الرئيسي فقط!' 
    });
    return;
}
```

### مستويات الوصول

```
كل المستخدمين ← فئات 1, 2, 3, 7
       ↓
المشرفين + ← فئات 1-4, 7
الأدمن
       ↓
الأدمن فقط ← فئات 1-7 (كلها)
```

---

## 💾 قاعدة البيانات

لا يتم حفظ أي بيانات جديدة في قاعدة البيانات. النظام يستخدم فقط:
- معلومات المستخدم الموجودة
- حالة المستخدم المؤقتة (`awaitingCommandsChoice`)

---

## 🎨 التنسيق البصري

جميع القوائم تستخدم:
- **رموز تعبيرية**: لكل فئة رمز فريد
- **فواصل بصرية**: أسطر ومربعات
- **ألوان نصية**: نصوص عريضة (`*`)
- **ترتيب منطقي**: من الأساسي للمتقدم

---

## ⚡ الأداء

### المميزات
- **سريع**: بدون استعلامات قاعدة بيانات حقيقية
- **خفيف**: الذاكرة تُحرر فوراً بعد الاختيار
- **موثوق**: فحص الصلاحيات لكل عملية

### استهلاك الموارد
```
[low]  ←  عرض القائمة
[low]  ←  معالجة الاختيار
[zero] ←  حفظ في قاعدة البيانات
```

---

## 🧪 الاختبار

### حالات الاختبار

#### ✅ حالة صحيحة
```
1. كتب /أوامر
2. اختار 1
3. ظهرت أوامر الأعضاء
```

#### ❌ حالة خطأ - رقم غير صحيح
```
1. كتب /أوامر
2. اختار 8 (غير موجود)
3. ظهرت رسالة خطأ
```

#### ❌ حالة خطأ - بدون صلاحيات
```
1. عضو عادي كتب /أوامر
2. اختار 5 (أوامر الأدمن)
3. ظهرت رسالة: "فقط الأدمن الرئيسي"
```

#### ⏱️ حالة انتهاء المهلة
```
1. كتب /أوامر
2. انتظر 5 دقائق
3. بعدها الاختيارات لن تعمل (يكتب /أوامر جديد)
```

---

## 🔔 رسائل الخطأ

| الموقف | الرسالة |
|-------|--------|
| رقم خارج النطاق | ❌ اختيار غير صحيح! اكتب أحد الأرقام من 1 إلى 7 |
| بدون صلاحيات (مشرف) | ❌ هذه الأوامر متاحة للمشرفين والأدمنز فقط! |
| بدون صلاحيات (أدمن) | ❌ هذه الأوامر متاحة للأدمن الرئيسي فقط! |

---

## 🔄 دورة حياة الجلسة

```
[1] استدعاء /أوامر
        ↓
[2] get JID + add to awaitingCommandsChoice
        ↓
[3] عرض القائمة الرئيسية
        ↓
[4] ننتظر رقماً (1-7)
        ↓
[5] messageHandler يفحص awaitingCommandsChoice
        ↓
[6] إذا رقم صحيح: معالجة + حذف من القائمة
[6] إذا خطأ: رسالة خطأ فقط (بدون حذف)
        ↓
[7] انتهت الجلسة
```

---

## 📊 الإحصائيات

### حجم الملف الجديد
```
commandsList.js: ~600 سطر
```

### خطوط الأوامر المحدثة
```
adminCommands.js: +2 أسطر (استيراد)
adminCommands.js: -60 سطر (كود أمر /أوامر القديم)
```

### زيادة الملفات التوثيقية
```
COMMANDS_MENU_GUIDE.md         : ملف جديد
COMMANDS_QUICK_REFERENCE.md    : ملف جديد
SYSTEM_ARCHITECTURE.md         : هذا الملف
```

---

## 🚀 التحسينات المستقبلية

### المرحلة التالية
- [ ] إضافة بحث عن أوامر معينة
- [ ] نظام الأوامر المتكررة
- [ ] حفظ المفضلة
- [ ] دعم لغات أخرى

### المراحل الأبعد
- [ ] واجهة رسومية موسعة
- [ ] نصائح ذكية لكل أمر
- [ ] سجل استخدام الأوامر
- [ ] لوحة معلومات مشرفين

---

## 📝 الملاحظات المهمة

### ✅ ما تم إضافته
- نظام تفاعلي جديد
- قوائم منظمة حسب الفئات
- فحص صلاحيات متقدم
- توثيق شامل

### 📌 ما لم يتغير
- أوامر البوت الأخرى كما هي
- قاعدة البيانات والإحصائيات
- نظام الألعاب
- الحماية والخصوصية

### ⚠️ نقاط الانتباه
- تأكد من إضافة `handleCommandsChoice` إلى المشاريع الأخرى
- تأكد من تحديث `awaitingCommandsChoice` في كل جلسة جديدة
- الرسائل الطويلة قد تحتاج تقسيم على دفعات

---

## 📞 الدعم التقني

للأسئلة حول النظام:
1. اقرأ `COMMANDS_MENU_GUIDE.md` للاستخدام
2. اقرأ `COMMANDS_QUICK_REFERENCE.md` للأوامر
3. اقرأ `SYSTEM_ARCHITECTURE.md` للتفاصيل التقنية (هذا)
4. تحقق من التعليقات في الكود

---

**آخر تحديث**: 10 مارس 2026  
**الإصدار**: النظام التفاعلي v1.0  
🍀 **كلوفر** - نظام إدارة مجتمعات الأنمي
