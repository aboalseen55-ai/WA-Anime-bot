# 🌟 نظام المنشن الجديد - دليل شامل

## 📝 نظرة عامة

تم استبدال نظام البحث القديم (الذي كان يعتمد على البحث بالاسم أو الرقم) بنظام منشن جديد محسّن يسمح بـ:
- تسجيل رقم الشخص (JID) تلقائياً عند عمل منشن
- ضمان تحديد الشخص الصحيح
- تسهيل عملية التفاعل عبر الهاتف

---

## 🔄 كيفية عمل النظام

### البنية الأساسية:

```
المستخدم يكتب أمر
    ↓
البوت يحفظ حالة انتظار (pending state)
    ↓
البوت يطلب: "قم بعمل منشن للشخص"
    ↓
المستخدم يرد بمنشن الشخص
    ↓
البوت يستخرج JID من المنشن
    ↓
البوت يسجل JID مع لقب الشخص في قاعدة البيانات
    ↓
البوت ينفذ الأمر (ترقية أو ترحيب)
```

---

## 🎯 الأوامر المدعومة

### 1. أمر الترقية (`/ترقية`)

**الاستخدام:**
```
1. /ترقية يوليوس آل كلوفر
2. البوت: "✋ الرجاء عمل منشن (@) للاعب يوليوس آل كلوفر"
3. ضع رد على الرسالة واكتب: @يوليوس أو @962795137282
4. اضغط على الشخص في القائمة
5. ✅ تم تسجيل الرقم وترقية الشخص
```

**البيانات المسجلة:**
- `jid`: معرّف واتساب الفريد
- `phoneNumber`: رقم الهاتف

---

### 2. أمر الترحيب (`/ترحيب`) - النظام الجديد

**المتطلبات:**
- الأمر يعمل **فقط** في مجموعة الاستقبال
- صاحب الأمر يجب أن يكون مشرف أو أدمن
- العضو يجب أن يكون معرّف لقبه في النظام

**الاستخدام الجديد:**
```
1. /ترحيب سارة علي
   ↓
2. البوت: "⏳ جارٍ البحث عن صور للعضو سارة علي..."
   ↓
3. البوت: يرسل 4 صور واحدة تلو الأخرى
   ↓
4. أنت: ترد برقم الصورة المفضلة (1️⃣ أو 2️⃣ أو 3️⃣ أو 4️⃣)
   ↓
5. البوت: يتحقق من وجود العضو في المجموعة الأساسية
   ↓
6. البوت: يرسل رسالة الترحيب إلى المجموعة الأساسية
   ↓
7. ✅ إعلام المسؤول بنجاح العملية
```

**مراحل العملية:**

#### المرحلة 1️⃣: البحث والعرض (في الاستقبال)
- البحث التلقائي عن صور للشخصية
- عرض 4 صور للاختيار
- رسالة توضيح بكل صورة

#### المرحلة 2️⃣: الاختيار (من المسؤول)
- المسؤول يختار صورة (1-4)
- البوت يتحقق من الاختيار

#### المرحلة 3️⃣: التحقق والإرسال
- التحقق من وجود العضو في المجموعة الأساسية
- إرسال الترحيب مع الصورة والمنشن
- الإعلام بنتيجة العملية

**رسالة الترحيب تتضمن:**
- 🍀 شعار المملكة وديكور
- اللقب: اسم الشخص المسجل
- المنشن: @رقم الشخص (مستخرج تلقائياً)
- المسؤول: اسم من أرسل الترحيب
- رابط الإعلانات الرسمي

---

## 🔄 نظام الحالات الجديد (Pending States)

### البيانات المحفوظة للترحيب:

```javascript
// حالة اختيار الصور
pendingMentions[`welcome_images_${groupJid}_${userJid}`] = {
    action: 'welcome_images',
    nickname: 'سارة علي',
    userJid: '962798123456@s.whatsapp.net',
    mentionPhone: '962798123456',
    moderatorName: 'يوليوس آل كلوفر',
    moderatorJid: '962776940071@s.whatsapp.net',
    imageUrls: [...],      // مصفوفة من 4 روابط صور
    receptionGroupJid: '120363425936664892@g.us',  // مجموعة الاستقبال
    mainGroupJid: '120363423609901756@g.us',       // المجموعة الأساسية
    kingdom: 'clover'
}

// حالة التأكيد بدون صور
pendingMentions[`welcome_confirm_${groupJid}_${userJid}`] = {
    action: 'welcome_confirm',
    nickname: 'سارة علي',
    userJid: '962798123456@s.whatsapp.net',
    mentionPhone: '962798123456',
    moderatorName: 'يوليوس آل كلوفر',
    moderatorJid: '962776940071@s.whatsapp.net',
    imageUrl: null,        // لا توجد صورة
    receptionGroupJid: '120363425936664892@g.us',
    mainGroupJid: '120363423609901756@g.us',
    kingdom: 'clover'
}
```

### مثال عملي:

```javascript
// عند كتابة /ترحيب فاطمة علي في الاستقبال
pendingMentions['welcome_images_120363425936664892@g.us_962798123456@s.whatsapp.net'] = {
    action: 'welcome_images',
    nickname: 'فاطمة علي',
    userJid: '962798123456@s.whatsapp.net',
    imageUrls: [
        'https://example.com/img1.jpg',
        'https://example.com/img2.jpg',
        'https://example.com/img3.jpg',
        'https://example.com/img4.jpg'
    ],
    ...
}

// عند رد المسؤول برقم 2
// ✅ يتم:
// 1. استخراج الصورة الثانية
// 2. التحقق من وجود فاطمة في المجموعة الأساسية
// 3. إرسال الترحيب مع الصورة والمنشن
// 4. إعلام المسؤول بالنتيجة
// 5. حذف الحالة المعلقة
```

---

## 🔧 الدوال الرئيسية

### 1. `extractAndSaveUserFromMention(sock, jid, mentionedJid, nickname)`

**الوصف:** استخراج JID من منشن وتسجيله

**المعاملات:**
- `sock`: كائن الاتصال بـ WhatsApp
- `jid`: معرّف المجموعة
- `mentionedJid`: JID الشخص المُشار إليه
- `nickname`: اسم الشخص للبحث عنه

**القيمة المرجعة:**
- كائن User مع البيانات المحدثة
- أو `false` في حالة الخطأ

**مثال:**
```javascript
const user = await extractAndSaveUserFromMention(sock, jid, mentionedJid, 'يوليوس');
if (user) {
    console.log(`✅ تم تسجيل: ${user.phoneNumber}`);
}
```

---

### 2. `getPhoneFromJID(jid)`

**الوصف:** استخراج رقم الهاتف من معرّف JID

**الصيغة:**
```
JID: 962795137282@s.whatsapp.net
Phone: 962795137282
```

**المثال:**
```javascript
const phone = getPhoneFromJID('962795137282@s.whatsapp.net');
console.log(phone); // 962795137282
```

---

## 📊 تدفق البيانات

### مخطط العملية:

```
┌─────────────────────────────┐
│  المستخدم يكتب /ترقية يوليوس   │
└────────────┬────────────────┘
             │
             ▼
┌─────────────────────────────┐
│  البوت يبحث عن "يوليوس"      │
│  في قاعدة البيانات          │
└────────────┬────────────────┘
             │
             ▼
┌─────────────────────────────┐
│  حفظ حالة انتظار:            │
│  pendingMentions[groupId]   │
│  action: 'promotion'        │
│  nickname: 'يوليوس'          │
└────────────┬────────────────┘
             │
             ▼
┌─────────────────────────────┐
│  طلب المنشن:                 │
│  "قم بعمل منشن للاعب"        │
└────────────┬────────────────┘
             │
             ▼
┌─────────────────────────────┐
│  المستخدم يرد بمنشن          │
│  contextInfo.mentionedJid   │
│  = '962795137282@s.whatsapp.net' │
└────────────┬────────────────┘
             │
             ▼
┌─────────────────────────────┐
│  البوت يستقبل الرسالة        │
│  مع بيانات المنشن            │
└────────────┬────────────────┘
             │
             ▼
┌─────────────────────────────┐
│  استخراج JID و Phone:       │
│  jid: '962795137282@...'    │
│  phone: '962795137282'      │
└────────────┬────────────────┘
             │
             ▼
┌─────────────────────────────┐
│  تحديث قاعدة البيانات:      │
│  user.jid = new JID        │
│  user.phoneNumber = phone  │
└────────────┬────────────────┘
             │
             ▼
┌─────────────────────────────┐
│  تنفيذ الأمر:               │
│  promoteModerator()         │
└────────────┬────────────────┘
             │
             ▼
┌─────────────────────────────┐
│  ✅ اكتمل الأمر بنجاح       │
│  حذف الحالة المعلقة         │
└─────────────────────────────┘
```

---

## ⚙️ التعديلات على الملفات

### 1. `messageHandler.js`
- إضافة `pendingMentions` object
- معالجة رسائل منشن
- استدعاء الدوال المناسبة حسب action

### 2. `adminSystem.js`
- إزالة: `createMention()`
- إضافة: `extractAndSaveUserFromMention()`
- حفظ JID و phoneNumber

### 3. `adminCommands.js`
- تحديث `/ترقية` لطلب منشن
- تحديث `/ترحيب` لطلب منشن
- إزالة استيراد `createMention`

### 4. `user.js`
- حذف أوامر `/منشن` و `/ابحث`
- إزالة استيراد `createMention` و `findUserByNicknameOrPhone`

---

## 🎓 أمثلة عملية

### مثال 1: ترقية عضو جديد

```
الأدمن: /ترقية أحمد

البوت: ✋ الرجاء عمل منشن (@) للاعب أحمد لتسجيل رقمه وإتمام الترقية.

الأدمن: [رد على الرسالة] @أحمد

النتيجة:
✅ تم تسجيل JID الخاص به
✅ تم حفظ phoneNumber: 966512345678
✅ تمت ترقيته إلى مشرف
```

### مثال 2: ترحيب عضو جديد

```
المشرف: /ترحيب فاطمة علي

البوت: ✋ الرجاء عمل منشن (@) للعضو الجديد فاطمة علي لتسجيل رقمها وإرسال رسالة الترحيب.

المشرف: [رد على الرسالة] @فاطمة

النتيجة:
✅ تم تسجيل JID: 966598765432@s.whatsapp.net
✅ تم حفظ phoneNumber: 966598765432
✅ تم إرسال رسالة ترحيب جميلة تتضمن:
   - الاسم (فاطمة علي)
   - رقم الهاتف (@966598765432)
   - اسم المسؤول الذي قام بالترحيب
```

---

## 🚨 معالجة الأخطاء

### حالات الخطأ المحتملة:

1. **لم يتم العثور على منشن:**
   ```
   البوت: ❌ لم يتم العثور على منشن صحيح!
   ```

2. **المستخدم غير مسجل:**
   ```
   البوت: ❌ لم يتم العثور على مستخدم باسم "يوليوس"!
   ```

3. **خطأ في استخراج JID:**
   ```
   البوت: ❌ حدث خطأ في معالجة المنشن!
   ```

---

## 💡 نصائح مهمة

1. **تأكد من المنشن الصحيح:** اختر الاسم/الرقم الصحيح من قائمة الاقتراحات
2. **استجب سريعاً:** الروابط المعلقة تنتظر ردك
3. **استخدم الاسم الكامل:** كلما كان الاسم أدق، كان البحث أسهل
4. **راجع قاعدة البيانات:** تأكد من أن البيانات محدثة بشكل صحيح

---

آخر تحديث: مارس 2026
